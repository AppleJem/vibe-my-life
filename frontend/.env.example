# Relative, so the API is whatever host serves the page and Vite's proxy forwards /api to
# the backend on :3001. Prefer this over an absolute address: a hard-coded LAN IP breaks
# every request the moment the machine's address changes.
VITE_API_URL=/api
