# Expense Search Feature — Implementation Todo

- [x] 1. Create `useDebounce` hook (`src/hooks/useDebounce.ts`)
- [x] 2. Create `MonthPicker` component (`src/components/ExpenseTracker/MonthPicker.tsx`)
- [x] 3. Create `SearchBar` component (`src/components/ExpenseTracker/SearchBar.tsx`)
- [x] 4. Create `useExpenseSearch` hook (`src/hooks/useExpenseSearch.ts`)
- [x] 5. Create `SearchResults` component (`src/components/ExpenseTracker/SearchResults.tsx`)
- [x] 6. Modify `Layout.tsx` — add search button + search bar slot
- [x] 7. Modify `index.tsx` — integrate search state and conditional rendering
- [x] 8. Verify build compiles
